import { API_KEY } from '../../Constants/API';
import { Endpoint, EndpointRoutes } from '../../Constants/Endpoints';
export const Data = {
  // This is the API key for the Amazon Product Advertising API
};
